# Konfiguracja Supabase

## URL Configuration

W panelu Supabase przejdź do **Authentication → URL Configuration** i ustaw:

- **Site URL**: adres domeny produkcyjnej (np. `https://replyai.pl`)

Jest to wymagane, aby linki w emailach (potwierdzenie konta, reset hasła) kierowały pod właściwy adres.
