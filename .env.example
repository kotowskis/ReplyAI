# ReplyAI — Szablon zmiennych środowiskowych
# Skopiuj ten plik: cp .env.example .env.local
# Uzupełnij wartości — NIE commituj .env.local do Git!
# Instrukcja jak zdobyć każdy klucz: patrz README.md

# ─── SUPABASE ────────────────────────────────────────────────────────────────
# supabase.com → twój projekt → Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── ANTHROPIC ───────────────────────────────────────────────────────────────
# console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=

# ─── STRIPE ──────────────────────────────────────────────────────────────────
# dashboard.stripe.com → Developers → API keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# dashboard.stripe.com → Developers → Webhooks → Add endpoint
STRIPE_WEBHOOK_SECRET=

# dashboard.stripe.com → Products → (stwórz produkty) → Price ID
STRIPE_PRO_PRICE_ID=
STRIPE_AGENCY_PRICE_ID=

# ─── RESEND ──────────────────────────────────────────────────────────────────
# resend.com → API Keys → Create API Key
RESEND_API_KEY=
# Adres nadawcy — wymaga zweryfikowanej domeny w Resend
RESEND_FROM_EMAIL=ReplyAI <noreply@replyai.pl>

# ─── SENTRY ─────────────────────────────────────────────────────────────────
# sentry.io → Settings → Projects → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# sentry.io → Settings → Auth Tokens (potrzebny do uploadu source map)
SENTRY_AUTH_TOKEN=sntrys_xxx

# Nazwa organizacji i projektu w Sentry
SENTRY_ORG=twoja-org
SENTRY_PROJECT=replyai

# ─── CRON ────────────────────────────────────────────────────────────────────
# Vercel → Settings → Environment Variables → wygeneruj losowy secret
# Używany do autoryzacji endpointów cron (/api/cron/*)
CRON_SECRET=

# ─── GOOGLE BUSINESS PROFILE ────────────────────────────────────────────────
# console.cloud.google.com → APIs & Services → Credentials → OAuth 2.0 Client
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Redirect URI po autoryzacji OAuth (musi odpowiadać konfiguracji w Google Cloud)
# Lokalnie: http://localhost:3000/api/auth/google/callback
# Na Vercel: https://twojadomena.vercel.app/api/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Klucz szyfrowania tokenów OAuth (32-byte, base64) — wygeneruj: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
GOOGLE_TOKEN_ENCRYPTION_KEY=

# ─── MOCK / DEV ────────────────────────────────────────────────────────────
# ⚠️ WYŁĄCZ PRZED PRODUKCJĄ! Ustawienie "true" włącza dane testowe zamiast Google API.
NEXT_PUBLIC_MOCK_GOOGLE_REVIEWS=true

# ─── APP ─────────────────────────────────────────────────────────────────────
# Lokalnie: http://localhost:3000
# Na Vercel: https://twojadomena.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000
